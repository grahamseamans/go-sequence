this page is another parent along with laynch view

it allows us to do settings 

we can:

assign different devices to tracks
assign midi ports and channels to the devices (the devices can also be thought of as columns in the launch view)

for drum devices you can set the drum machine type to send out the right notes.

keyboard input routing so taht for tonal devices you can play them when it's set to preview