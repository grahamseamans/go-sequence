so this is in a way one of the parent views

it has a grid of devices (8 devices)
each device has a bunch of patterns (rows)
this makes a grid

you can queue a whole row of patterns by hitting the pad on the left side of the device (the one next to the row)

if nothing playing, and you hit play, it will play immeditatly
but otherwise it will be queued
we defer to ableton's conventions for how this works with different length sequences and similar edge cases