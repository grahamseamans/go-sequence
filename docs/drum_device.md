the drum device

I think that
top two quadrants of the controller mades 8x4 (32) long sequence
bottom left chooses what note is selected to be squenced (or played if in preview) (4x4 so 16)


bottom right is tons of commands

lane length pad - hold pad then hit a pad on the sequcner top half. this is how long that voices pattern is
record pad (toggle) - when on and you play a note (hitting onf of the pads in bottom left quadrant) it gets written to the sequence
preview button (toggle) - when toggled if you hit a pad on the bottom left it triggers that sound
