package system

// Settings holds UI state for the per-track settings editor.
// Currently empty — the settings view reads/writes Track fields directly
// and has no transient widget state of its own. Defined here to keep the
// layer structure uniform.
type Settings struct{}
