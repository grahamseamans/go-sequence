package system

// Empty holds UI state for the "no device assigned" placeholder view.
// Currently empty — the Empty view renders a static placeholder and has
// no transient state. Defined here to keep the layer structure uniform.
type Empty struct{}
