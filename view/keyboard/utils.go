package keyboard

// Package keyboard is the external-MIDI-keyboard view surface.
//
// This file is the home for cross-domain keyboard helpers. Nothing
// universal lives here yet; domain-specific helpers stay inside their own
// per-domain files (drum.go, looper.go).
//
// The top-level MIDI dispatcher + RunRoutine lives in keyboard.go.
